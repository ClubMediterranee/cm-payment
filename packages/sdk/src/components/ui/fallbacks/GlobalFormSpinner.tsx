import { Spinner } from '@clubmed/trident-ui/molecules/Spinner';

export const GlobalFormSpinner = () => {
  return (
    <div className="flex items-center justify-center p-40">
      <Spinner className="w-68" />
    </div>
  );
};
